services:
  pgvector:
    image: pgvector/pgvector:pg16
    env_file:
      - .env.postgres
    ports:
      - "5432:5432"
    volumes:
      - pgvector_data:/var/lib/postgresql/data

volumes:
  pgvector_data: